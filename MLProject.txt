name: graduate_admission_project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      train_path: {type: str, default: "dataset_preprocessing/train_clean.csv"}
      test_path: {type: str, default: "dataset_preprocessing/test_clean.csv"}
    command: >
      python modelling.py --train_path {train_path} --test_path {test_path}
