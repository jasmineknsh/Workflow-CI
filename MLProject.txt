name: graduate_admission_project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      train_path: {type: str}
      test_path: {type: str}
    command: >
      python modelling.py
      --train_path {train_path}
      --test_path {test_path}
